import { EmployeesClient } from '@/components/app/employees-client';

export default function EmployeesPage() {
  // Esta página de servidor apenas renderiza o componente de cliente
  return <EmployeesClient />;
}