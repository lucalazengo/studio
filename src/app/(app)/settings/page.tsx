import { Card, CardHeader, CardTitle } from '@/components/ui/card';

export default function SettingsPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Configurações</CardTitle>
      </CardHeader>
      {/* O conteúdo da sua página de configurações virá aqui */}
    </Card>
  );
}